// tslint:disable-next-line: class-name
export class column {
    // kiểu dữ liệu
    // 0: defaul ; 1: number; 2:select box; 3: date; 4: status
    type?: number;

    // tên cột
    name: string;

    // formatData: kiểu hiển thị dữ liệu
    formatData?: number;

    //
    dataIndex: string;


}